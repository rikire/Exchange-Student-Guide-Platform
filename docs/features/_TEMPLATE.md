---
id: FEAT-XXX
title: Short name of the feature
status: planned
covers: [FR-XXX]
slice: ""
routes: []
tables: []
code: []
tests: []
---

# FEAT-XXX — Short name of the feature

> Created by `/feature`. The front matter is read by the matrix generator, so the fields must
> reflect reality rather than intent.

## Why

Which problem this solves for a reader, a contributor or the OGE moderator. One or two paragraphs,
without retelling the implementation.

## Scenario

What it looks like from the actor's side, step by step. Reference CJM steps (`UC-XXX`) where this
feature is part of a journey.

## Routes

| Method and path | Purpose | Template |
|---|---|---|

## Schema impact

Which tables are touched, which columns are added, which indexes are needed and for which queries.

## Acceptance criteria

- [ ] A checkable statement, not "works correctly". Each one becomes a test whose name repeats it.

## Deliberately out of scope

An explicitly drawn boundary. An item here either becomes its own feature or is recorded as `CON-XXX`.

## Open questions

Must not be left empty: if there are none, write that there are none.
