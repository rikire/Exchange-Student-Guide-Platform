# Session 5a1a7d34 — 2026-09-06

Written by the `hook` commands of `ai-tools`, not by hand.

## 20:18

**Author:** Mikhail Novikov

**Prompt**

> посмотри как в репо выстроена работа с ИИ, поищи в интернете скилы и инструкции связанные с тем, как выстраивать работу с ИИ в проекте, проведи аудит и предложи улучшения / изменения / добавления.

**Prompt (English)**

> Look at how work with AI is set up in this repository, search the internet for skills and instructions about how to structure work with AI in a project, carry out an audit and propose improvements / changes / additions.

**Outcome**

> Audited the AI-collaboration layer against current Claude Code mechanisms and Anthropic's published guidance, then implemented the agreed changes across nine commits. Found and fixed a critical hole: every hook ran a jar that target/ keeps out of git, and Claude Code treats a failing hook as non-blocking, so a fresh clone ran with the journal, protected-path guard, bypass refusal and documentation gate all silently off. Verified by moving the jar aside. Added client-enforced permissions.deny that survives an absent jar, a SessionStart reporter written as plain shell so it can run when the jar cannot, and  paths. Migrated eleven slash commands from the legacy .claude/commands to skills, gaining real command-output injection for /dod and /sync-docs, isolated context for the two read-heavy audits, and person-only invocation for the seven that write into the record. Added five path-scoped rule files so the Java, testing, security, schema and slice rules load when a matching file is opened. Added a dod-reviewer subagent so the Definition of Done is not graded by the context that wrote the code, and evals for three skills - written, not yet run, and documented as such. Switched off Claude Code auto memory because it is machine-local and outside git while the journal claims completeness. Added AGENTS.md, a status line, a PreCompact journal marker, and a rule against accumulating fallbacks. Rejected an LLM-judgment guard and a CI review agent on determinism and cost, rejected a PostToolUse formatting hook after timing it at 5.5 seconds per edit, and deferred plugin packaging as DEBT-002. Recorded everything in docs/ai/audit-2026-09-06.md, including what the audit does not establish. scripts/check.sh passes; nothing pushed.

**Checks**

- traceability gate: not enabled yet (phase 2)
- English rendering: supplied
- documentation gate: passed

