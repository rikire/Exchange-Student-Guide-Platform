# Session 998ab12b — 2026-09-05

Written by the `hook` commands of `ai-tools`, not by hand.

## 21:37

**Author:** Mikhail Novikov

**Prompt**

> <ide_selection>The user selected the lines 79 to 79 from c:\study\PAI\Exchange-Student-Guide-Platform\.claude\settings.json:
> doc
> 
> This may or may not be related to the current task.</ide_selection>
> давай придумаем как можно добавить инструкцию не писать велосипеды, не изобретать самопальные куски кода, а поискать подходящие библиотеки и подумать над тем, что будет лучше - использовать библиотеку или написать самому.
> и добавь в секурити проверку на n+1 проблему. 
> давай еще подумаем, во время написания кода, какие решения ИИ должен принимать сам, а какие человек?

**Prompt (English)**

> Let us work out how to add an instruction not to build wheels - not to invent home-made pieces of code, but to look for suitable libraries and think about whether using a library or writing it ourselves is better. Also add an N+1 check to the security document. And let us think about which decisions the AI should take on its own while writing code, and which the human should take. [Then, across several rounds:] the human should take an active part in code-level decisions, especially anything touching the domain layer, the database and security; validation should always be in the domain and elsewhere only if needed or if it speeds something up; write a detailed instruction on how to write tests; cover all the corner cases, search the internet, throw in more options; add something that tells the model when it is better to go and look information up on the internet; and add that for a hard task needing invention, go and look at existing solutions - writing from memory or inventing a wheel may not be great.

**Outcome**

> Found that the wheels problem was economic rather than attitudinal: the authority split made adding a dependency the human's decision and writing a private helper the agent's, so asking cost a round trip and writing cost nothing, and the cheapest path always ran away from the library. Fixed both halves - replacing a library with our own code is now the same decision as adding one, and a hook asks at the moment such a file is created, for anything under shared/, in the schema or security packages, or named like a wheel. Folded three test rules into the same hook: a disabled test with no debt entry and a sleep in a test are refused, a test that asserts nothing is questioned. Every rule anchored to the start of a line so it survives its own test data, which the previous two rules of this shape had not. Wrote docs/ai/testing.md with a real-database spine and a corner-case checklist organised by input rather than by class. Two searches grounded it and one of them corrected the repository's own security document, which was presenting magic bytes as settling the file type when a polyglot walks through them. Made N+1 a security requirement on the threat model rather than a performance note, and booked its gate into phase 2 where the persistence layer arrives. Added the decision table to collaboration.md with two new criteria, and a section on when to go and look something up - including the trigger that is missed most often, a design problem that feels tractable. 94 tests, eleven verification probes through the real hook, check.sh green. The push could not reach GitHub.

**Checks**

- traceability gate: not enabled yet (phase 2)
- English rendering: supplied
- documentation gate: passed

