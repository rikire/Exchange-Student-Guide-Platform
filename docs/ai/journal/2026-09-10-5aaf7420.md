# Session 5aaf7420 — 2026-09-10

Written by the `hook` commands of `ai-tools`, not by hand.

## 01:02

**Author:** Mikhail Novikov

**Prompt**

> <ide_opened_file>The user opened the file c:\study\PAI\Exchange-Student-Guide-Platform\docs\ai\security.md in the IDE. This may or may not be related to the current task.</ide_opened_file>
> почему это вообще было записано в security.md, это же архитектурное решение, а не инструкция для ИИ.

**Prompt (English)**

> Why was this written into security.md at all - it is an architectural decision, not an instruction for the AI.

**Outcome**

> Agreed, and established the cause from git rather than guessing: docs/ai/security.md was created on 3 September in the same commit as the rest of the AI instructions, three days before the first ADR existed, so there was no architecture-decision home when the security rules were written. A second, structural cause was in the repository's own rules - constraints.md routed every 'framework-level or implementation detail' to docs/ai/architecture-rules.md, pointing architecture into the assistant's instruction directory. Asked the human how to resolve it and they chose to split by audience. Carried that out: created docs/architecture/security.md holding the uploads, article-content, forms-and-abuse, reads, admin-area and secrets rules, each citing the ADR that decided it; wrote ADR-0009 (one shared password for the admin area rather than accounts, decided 3 September) and ADR-0010 (bounded reads and chosen fetch strategy, decided 5 September), both with the Decided/Recorded date pair; cut docs/ai/security.md down to the three genuinely agent-facing sections plus pointers. Fixed the two defects found while moving, both of which were instances of the exact problem raised - the allowlist had lost audio, which CON-006 accepts, and the document still required an HTML sanitizer that ADR-0001 had made unnecessary by choosing Markdown. Corrected the routing rule in constraints.md to send architectural decisions to an ADR, structural conventions to architecture-rules.md, and only assistant instructions to docs/ai/. Updated repository-map.md and struck the two now-resolved rows from the ADR audit table. docs-check green throughout.

**Checks**

- traceability gate: not enabled yet (phase 2)
- English rendering: supplied
- documentation gate: passed


## 01:09

**Author:** Mikhail Novikov

**Prompt**

> out диаграмм стоит тоже отправить в репозиторий, чтобы их можно было предпросматривать и вставлять в документацию.

**Prompt (English)**

> The diagrams' out directory should be committed to the repository as well, so they can be previewed and embedded in the documentation. [Earlier in the same turn: once you finish your task, we will need to go over the whole AI working process and the files again, because it looks like a real mess. Write that down. For now, go back to the task.]

**Outcome**

> Recorded the requested process review as IB-004 in docs/ai/instruction-backlog.md, grounded in the three defects found this session - the retroactive ADRs, the security architecture filed in the assistant's instruction directory, and the constraints.md rule that routed it there - and noted that the two stale rules those produced survived every green build because docs-check verifies links and commands, not whether claims still hold. Then returned to the plan. Wrote scripts/diagrams.sh, which fetches PlantUML 1.2026.8 into a gitignored cache, verifies it against a SHA-256 computed by actually downloading it, and renders with -failfast2 so a diagram error exits non-zero instead of producing a picture of a stack trace. Wrote four PlantUML sources - C4 levels 1 to 3 and the ERD - and rendered all four, checking each SVG has real text nodes and no error markers. Wrote docs/architecture/overview.md and docs/architecture/data-model.md from the ADR decisions, each table naming the ADR that decided it. On the request about the out directory: found the cause was a generic 'out/' build-output pattern at .gitignore line 30 catching docs/diagrams/out incidentally, not a decision about diagrams; added an explicit negation, and recorded in docs/diagrams/src/README.md and docs/repository-map.md that this output is committed although still never edited by hand. Added scripts/diagrams.sh to the executable-bit list in check.sh. Full scripts/check.sh passes. Flagged two things for the human rather than deciding them: the entity fields, key types and enum values now written into data-model.md are the human's decision under .claude/rules/schema.md and several are mine rather than derived from a requirement, and docs-sync.md's claim that every generated file carries a GENERATED marker does not hold for a PlantUML SVG.

**Checks**

- traceability gate: not enabled yet (phase 2)
- English rendering: supplied
- documentation gate: passed


## 01:17

**Author:** Mikhail Novikov

**Prompt**

> проведи ревью диаграмм и дата модели опираясь на требования и дизайн
> мелкое противоречие - исправь

**Prompt (English)**

> Review the diagrams and the data model against the requirements and the design. The small contradiction - fix it.

**Outcome**

> Fixed the contradiction, and found it was wider than reported: docs-sync.md claimed every generated file carries a GENERATED marker and that a hand edit is detected and breaks the build. Grepping tools/src found nothing reads the marker for any file, so the enforcement claim was false for all five entries, not only for SVGs. Made scripts/diagrams.sh stamp the marker into each SVG on its own line before the root element, and corrected docs-sync.md to say the marker is a convention with no check behind it yet, pointing at phase 2 and at IB-003, which identifies exactly this class of rule. Then reviewed all four diagrams and the data model against every FR, every NFR, every CON and all eleven screens. Corrected five real errors. The C4 level-3 diagram showed no slice touching the database - only shared to db - which inverted ADR-0002's decision that each slice declares its own repository; added the per-slice arrows, leaving wikilink as the only slice without one, which now encodes the ArchUnit rule visually. Added the missing reindex paths from contribute (FR-023, FR-024 direct publish) and backup, plus articleview to media and backup to both stores. Fixed an ERD cardinality error: article to submission was drawn zero-or-one, but nothing stops two contributors proposing edits to the same article and FR-014 lists every pending submission. Corrected an overclaim on level 1 and in overview.md that there is nothing to integrate with - CON-008 settles inbound only, and ADR-0008 leaves a hosted CAPTCHA provider open, which would be an external system. Removed the submitter_ip_hash column I had invented: it contradicts ADR-0008, which left the counting mechanism to the implementation, and could not have served FR-013 anyway because a submission is rejected before any row exists. Recorded three open schema decisions in data-model.md rather than deciding them, since entity fields and cardinality are the human's call - FR-026 removal has no representation at all and a hard delete would take FR-020's retained revisions with it; the pinned boolean cannot order two pinned articles although the landing screen shows exactly that; and where the rate limit is counted. Recorded two design gaps in docs/design/README.md: neither the submission form nor the submission review screen has a summary field although FR-010, FR-011 and FR-017 now require one, and nothing on the article screen reaches propose-an-edit. The review also confirmed the two-timestamp decision independently - the landing screen shows 'Added N ago' for recent articles and 'Updated N ago' for pinned ones. Full scripts/check.sh passes.

**Checks**

- traceability gate: not enabled yet (phase 2)
- English rendering: supplied
- documentation gate: passed

