# Session 998ab12b — 2026-09-04

Written by the `hook` commands of `ai-tools`, not by hand.

## 15:05

**Author:** Mikhail Novikov

**Prompt**

> пробегись по фазе 0 и посмотри все ли что нужно сделано и что можно улучшить

**Prompt (English, added 2026-09-04, retrospectively)**

> Go over phase 0 and see whether everything needed is done, and what could be improved.

**Outcome**

> Проверено фактами, не по памяти. Критерий готовности фазы 0 держится: `scripts/check.sh` зелёный, 36 тестов, CI успешен на последних четырёх прогонах. Но нашлось восемь расхождений, и большинство — того самого сорта, который репозиторий заявлял, что не допускает.
> 
> ## Документация врёт про код — правило 5, которое я же и написал
> 
> **1. `CLAUDE.md` рекламирует три несуществующие команды** — `/ownership`, `/gap-list`, `/viva-prep`. Агент их вызовет и получит ошибку.
> 
> **2. `/dod` сегодня падает на середине.** Он велит выполнить `ai-tools trace --docs-sync HEAD`, которого нет. У `/trace-check` есть запасной путь («если генератор не собран, проверь чтением»), у `/dod` — нет. То же в `definition-of-done.md`, пункты 6 и 7. Это не отложенная фича, а сломанный чек-лист готовности.
> 
> **3. Хук `PostToolUse` описан в `repository-map.md` как работающий** — «подсказывает, какую документацию обновить». В `.claude/settings.json` его нет. Строка в таблице автоматики просто неправда.
> 
> **4. Четыре битые ссылки в `docs/course/rubric.md`** — на `design-doc.md`, `mid-demo.md`, `final.md`. Это документ, который будет читать оценщик.
> 
> **5. `scripts/diagrams.sh`** упомянут как способ обновить диаграммы. Файла нет.
> 
> **6. `docs/team/members.yml:20` — голый `# TODO:` без `DEBT-XXX`.** Прямо запрещено в «What you must not do» в `CLAUDE.md`.
> 
> ## Мёртвый код — мой же DoD велит удалять
> 
> **7. Четыре метода в `tools/` объявлены и ни разу не вызваны:** `markReminded`, `lastStopHash`, `setStopHash`, `Snapshot.fingerprint`. Все написаны «на будущее» — под `PostToolUse`-напоминания и гейт `Stop`, то есть под фазу 2. `definition-of-done.md` §10 говорит про такое: «Код, добавленный на будущее и не использованный сейчас, — удалить».
> 
> ## Свежий клон получает не то, что описано
> 
> **8. Шести каталогов в репозитории нет** — git не хранит пустые: `templates/`, `static/`, `db/migration/`, `data/seed/`, `docs/diagrams/src`, `docs/diagrams/out`. Сборка не ломается, но `repository-map.md` описывает раскладку, которой у клонирующего не будет.
> 
> ## Не дефекты, но факты для честности
> 
> **Журнал: 8 записей, 0 с автором.** Атрибуция приехала после того, как фаза 0 была отработана. Восстановить задним числом нельзя и не нужно — но весь процессный каркас в журнале безымянный.
> 
> **Недельного лога за 2026-W36 нет.** Курс требует его на каждом этапе, а вся фаза 0 уложилась именно в эту неделю. Пустая папка с `README.md` — это не «ещё не начали», это пропущенная неделя.
> 
> **Два открытых чекбокса:** почта Абдирахима и стартовые статьи. Первый блокирует атрибуцию его коммитов, второй — главный риск демонстрации.
> 
> ---
> 
> ## Что предлагаю сделать
> 
> Пункты 1–6 и 8 — починка расхождений, полчаса, и после неё фазу 0 можно честно закрывать. Пункт 7 (мёртвый код) я бы **не удалял**, а завёл `DEBT-002`: методы понадобятся через неделю, а удалять и переписывать — хуже, чем записать. Хотя это ровно тот компромисс, который мой же DoD запрещает, поэтому решение ваше.
> 
> Недельный лог за W36 стоит завести сейчас — но авторство в нём ваше, и заполнять его за вас я не должен.
> 
> Что из этого делаю? Могу всё 1–6 и 8 одним заходом, или скажите, что отложить.

**Outcome (English, added 2026-09-04, retrospectively)**

> Audited phase 0 against its own criteria. The readiness criterion held - check.sh green, 36 tests, four passing CI runs - and eight divergences were there anyway. Seven were documentation describing what the repository did not contain: three advertised slash commands that did not exist, /dod calling a subcommand never built, the PostToolUse hook listed as live automation while unwired, four dead links in the course rubric, a named script that was absent, and a bare TODO with no debt reference. The eighth was four unused methods, plus six directories missing from a fresh clone.

**Checks**

- traceability gate: not enabled yet (phase 2)


## 15:15

**Author:** Mikhail Novikov

**Prompt**

> исправляй, заодно предложи, что можно делать в следующий раз, чтобы этих ошибок не допустить.

**Prompt (English, added 2026-09-04, retrospectively)**

> Fix them, and also suggest what to do next time to avoid these mistakes.

**Outcome**

> Запушено. Все восемь расхождений закрыты, `main` на `653de89`, 36 тестов зелёные.
> 
> ## Что чинилось
> 
> | | Как |
> |---|---|
> | Три несуществующие команды в `CLAUDE.md` | Убраны из списка; отложенные названы без бэктиков, чтобы не читались как вызываемые |
> | `/dod` падал на `ai-tools trace` | Теперь прямо говорит, что генератор в фазе 2, отсылает к проверке чтением и **требует написать в вердикте, что пункт проверен руками** |
> | `PostToolUse` описан как рабочий | Помечен как невключённый |
> | Четыре битые ссылки в `rubric.md` | Созданы `design-doc.md`, `mid-demo.md`, `final.md` |
> | `scripts/diagrams.sh` | Ссылка заменена: рендерер приедет с диаграммами в фазе 1 |
> | Голый `TODO` в `members.yml` | Снят — совпало с правкой Абдирахима |
> | Четыре мёртвых метода | **Удалены**, не спрятаны за `DEBT-002` |
> | Шесть пустых каталогов | READMEs, свежий клон получает описанную раскладку |
> 
> ## Механизм, а не обещание
> 
> Написав в роадмапе «`check.sh` теперь проверяет ссылки», я поймал себя на том же дефекте, который аудировал, — заявление без реализации. Поэтому проверка добавлена: относительные markdown-ссылки должны резолвиться, а команда, названная в инструкциях, должна существовать файлом. **Проверено подсадкой битой ссылки** — гейт упал.
> 
> Он поймал бы пять находок из восьми. Полноценный `trace` в фазе 2 его поглотит; до тех пор работает дешёвая версия на каждой сборке.
> 
> ## Абдирахим запушил параллельно — и там расхождение
> 
> Пока я работал, он зарегистрировал свою почту. Ребейз прошёл, конфликт в `members.yml` разрешил в его пользу: мой комментарий говорил «ещё не записано», что стало неправдой.
> 
> **Но его правка не работает.** В реестре `abdirahim@mail.ru`, а закоммитил он как `abdraoper@gmail.com`. Атрибуция для него всё ещё провалится. Чинится одним из двух, и это его выбор: добавить `abdraoper@gmail.com` в реестр, либо сменить `git config user.email`. Сам не стал — гадать, какая почта каноническая, значит записать за него решение.
> 
> ## Что делать в следующий раз
> 
> **Дефект был одной формы, а не восемью.** Все восемь жили в зазоре между документом и тем, что он описывает, и все были невидимы зелёной сборке. Отсюда три вывода:
> 
> **1. Заявляя в документе, что механизм есть, — включай его в том же ходе.** Я нарушил это дважды за час: сначала описав `PostToolUse` как рабочий, потом написав про проверку ссылок до её появления. Правило 5 говорит «изменил поведение — обнови документацию», но обратное направление не покрыто: документация опередила код. Стоит дописать в `docs-sync.md`, что расхождение считается дефектом **в обе стороны**.
> 
> **2. Не откладывай гейт, который проверяет отложенное.** Фаза 0 задекларировала дисциплину, которую сама не могла проверить: линк-чек и трейсабилити ушли в фазу 2. Дешёвая версия должна появляться вместе с правилом — иначе неделя работы копит расхождения молча.
> 
> **3. Аудит фазы — отдельный шаг критерия готовности, а не жест доброй воли.** Этот я сделал, потому что вы попросили. Предлагаю дописать в `docs/ai/roadmap.md`, что фаза не закрывается, пока по ней не прогнан аудит вида «каждое утверждение документа проверено против репозитория» — с датой и находками. Правка инструкций, поэтому жду вашего слова.
> 
> Незакрытым в фазе 0 остался один пункт: стартовые статьи.

**Outcome (English, added 2026-09-04, retrospectively)**

> Fixed all eight. Deleted the dead code rather than parking it behind a debt entry, because Definition of Done already says code written for later is deleted and proposing an exception to it was the wrong instinct. Created the three missing course documents, corrected the repository map, replaced the bare TODO, and added a README to each directory git could not carry because it does not track empty ones. Added a link and slash-command check to scripts/check.sh, verified by planting a broken link. Proposed three lessons: declare a mechanism only in the turn that builds it, do not defer the gate that checks deferred work, and make the phase audit a step rather than a favour.

**Checks**

- traceability gate: not enabled yet (phase 2)


## 15:18

**Author:** Mikhail Novikov

**Prompt**

> и что можно сделать с этими выводами?

**Prompt (English, added 2026-09-04, retrospectively)**

> And what can be done with these conclusions?

**Outcome**

> Измерил — проверка сработала бы прямо сейчас на четырёх реальных вещах: `trace`, `ownership`, `gaps` цитируются в документах, но не диспетчеризуются, и `PostToolUse` описан, но не подключён.
> 
> ## Что предлагаю
> 
> **A. Вывод 1 → проверка, а не абзац.** Три сравнения в `check.sh`, все дешёвые:
> 
> | Что сравнивать | Поймало бы |
> |---|---|
> | Подкоманды `ai-tools`, упомянутые в документах, против `case` в диспетчере | `trace`, `ownership`, `gaps` |
> | Файлы `scripts/*.sh`, упомянутые в документах, против существующих | `diagrams.sh` |
> | Хуки, названные в таблице автоматики, против `.claude/settings.json` | `PostToolUse` |
> 
> Тут есть тонкость, и она удачная: **соглашение уже появилось само**. Чиня находки, я пометил будущее как `_(phase 2)_` и `# phase 2`. Проверке остаётся отличать «рекламируется как рабочее» от «помечено как будущее» по этой пометке — то есть она узаконивает то, что уже написано, а не вводит новую церемонию.
> 
> Вместе с уже добавленной проверкой ссылок и команд это накрывает **семь находок из восьми**. Восьмая — мёртвый код — ловится иначе: линтер на неиспользуемые приватные методы. Но мои были `public`, так что не поймал бы никто; это остаётся на ревью diff'а из DoD §10.
> 
> **B. Вывод 2 → ничего не добавлять.** «Не откладывай гейт, который проверяет отложенное» — это принцип планирования, и я его уже применил, вытащив линк-чек из фазы 2 в фазу 0. Писать под него правило значит писать текст про то, что текст не работает. Предлагаю оставить как запись в роадмапе — она там уже есть.
> 
> **C. Вывод 3 → одна строка, не документ.** В `docs/ai/roadmap.md`, в раздел «Closing a phase»: фаза не закрывается, пока по ней не прогнан аудит «каждое утверждение документа проверено против репозитория», с датой и находками — включая «находок нет».
> 
> Это единственная из трёх вещей, которую механизмом не заменить: проверка ловит ссылки и имена, но не ловит, например, «PostToolUse описан как работающий» на уровне смысла или устаревшее описание поведения. Человеческий проход нужен, но раз в фазу, а не постоянно.
> 
> ## Чего делать не стоит
> 
> **Не буду дописывать в `docs-sync.md` правило «расхождение считается дефектом в обе стороны»** — вчера я это предлагал, сегодня считаю ошибкой. Правило уже есть в общем виде, а конкретный случай закрывается проверкой A. Ещё один абзац только разбавит документ, который и так на 66 строк.
> 
> ## Оценка честно
> 
> A — код, полчаса, проверяется подсадкой поломки. C — одна строка, но правка инструкций, нужно ваше слово. B — ноль работы.
> 
> Делать A и C? Или только A — тогда аудит фазы останется на вашем «пробегись», как сегодня, и это тоже рабочий вариант, просто он держится на том, что вы вспомните попросить.

**Outcome (English, added 2026-09-04, retrospectively)**

> Argued against my own instinct first: two of the three conclusions wanted to become more prose, which is the wrong answer to documentation drift in a repository that already has twelve instruction files. Measured what a mechanical check would catch - trace, ownership and gaps cited but not dispatched, PostToolUse documented but unwired - and proposed converting lesson one into checks, lesson two into nothing because it had already been acted on, and lesson three into a single line in the roadmap guide.

**Checks**

- traceability gate: not enabled yet (phase 2)


## 15:30

**Author:** Mikhail Novikov

**Prompt**

> делай

**Prompt (English, added 2026-09-04, retrospectively)**

> Do it.

**Outcome**

> Запушено, `main` на `6da16b6`, 49 тестов зелёные.
> 
> ## A — вывод 1 стал механизмом
> 
> `ai-tools docs-check` проверяет пять вещей: резолвятся ли относительные ссылки, есть ли файл у рекламируемой слэш-команды, диспетчеризуется ли цитируемая подкоманда, существует ли названный скрипт, подключён ли хук, который карта подаёт как автоматику.
> 
> **Написал на Java, а не третьей шелл-проверкой** — это `links`, который планировался в фазу 2, вытащенный вперёд. То есть вывод 2 применён, а не пересказан: правило, написанное раньше гейта, неделю копит то, что запрещает.
> 
> Соглашение «неготовое помечается фазой на своей же строке» существовало до проверки — теперь оно несущее. `Commands` держит таблицу диспетчеризации как источник истины, чтобы чекер и `switch` не разъехались.
> 
> **Две ошибки первой версии, обе поймал запуском, а не рассуждением:**
> 
> - Читала имя инструмента в прозе как вызов и сообщала о подкомандах `jar` и `with`. Проверка, которая кричит волками, — проверка, которую выключают.
> - **Проверяла журнал.** Это запись, которая законно фиксирует, что чего-то не было. Ронять сборку из-за неё значит приглашать править свидетельство под чекер. Исключил, и тест объясняет почему.
> 
> Подсадил все четыре вида поломки — ловятся; 13 тестов.
> 
> ## C — аудит стал шагом
> 
> В `docs/ai/roadmap.md`: фаза не закрывается, пока по ней не пройден аудит, находки в файл фазы, включая «нет». С обоснованием: фаза 0 прошла свой критерий на зелёной сборке и четырёх CI, и аудит всё равно нашёл восемь.
> 
> Проверка не заменяет аудит: она сравнивает имена и ссылки, а аудит читает смысл. Хук, описанный как делающий не то, что делает, пройдёт любую автоматику.
> 
> **B — ничего.** Уже применено делом.
> 
> ## Два наблюдения про Абдирахима
> 
> Он дважды пушил параллельно. Первый раз записал `abdirahim@mail.ru`, хотя коммитит как `abdraoper@gmail.com` — я это отметил, и вторым коммитом он исправил. Атрибуция теперь заработает.
> 
> **Но его коммит `fix: correct Abdirakhim's git email` наша конвенция отвергает** — `fix` требует scope `FEAT-XXX` и ссылку `[FR-XXX]`. Прогнал его сообщение через гейт: код 1. Значит **у него не установлены git-хуки** — `scripts/hooks.sh` не запускался.
> 
> Это не его невнимательность, а дыра в онбординге: хуки лежат в репозитории, но включаются вручную, и забыть это ничего не мешает. Стоит ему сказать про `scripts/hooks.sh`. Если такое повторится — чинить надо не человека, а установку.
> 
> Фаза 0 закрыта, кроме стартовых статей.

**Outcome (English, added 2026-09-04, retrospectively)**

> Built `ai-tools docs-check` in Java rather than as a third shell check: it is the links subcommand the plan had put in phase 2, brought forward, which is lesson two applied rather than restated. It verifies that links resolve, advertised slash commands exist, cited subcommands are dispatched, named scripts exist and documented hooks are wired, excusing work whose own line names its phase. Two faults of the first version were caught by running it: it read the tool's name in prose as an invocation, and it checked the journal, which is append-only evidence that must never be edited to satisfy a checker. Thirteen tests; the audit step went into the roadmap guide.

**Checks**

- traceability gate: not enabled yet (phase 2)


## 15:32

**Author:** Mikhail Novikov

**Prompt**

> <ide_opened_file>The user opened the file c:\study\PAI\Exchange-Student-Guide-Platform\docs\roadmap\01-requirements-design.md in the IDE. This may or may not be related to the current task.</ide_opened_file>
> у него макбук

**Prompt (English, added 2026-09-04, retrospectively)**

> He has a MacBook.

**Outcome**

> Проверил, а не предположил: **макбук почти ничего не меняет.**
> 
> Скрипты POSIX-чистые — ни `sed -i`, ни `readlink -f`, ни `grep -P`, ни `stat -c` в них нет, так что `check.sh`, `hooks.sh` и git-хуки на BSD-утилитах отработают как есть. Более того, `core.filemode` на macOS по умолчанию `true`, то есть исполняемые биты, которые я проставил в индекс, у него сработают лучше, чем у вас на Windows.
> 
> Ему нужно ровно две вещи: **Temurin 21** (`brew install --cask temurin@21`) и **`scripts/hooks.sh`**.
> 
> ## Но его коммит вскрыл другое, и это не про macOS
> 
> Гейт конвенции не сработал по **двум** причинам сразу, и обе независимы от ОС:
> 
> 1. Локальные хуки у него не установлены — `core.hooksPath` ставится вручную скриптом, и забыть это ничего не мешает.
> 2. **CI проверяет сообщения коммитов только на pull request.** А мы пушим прямо в `main`.
> 
> Два промаха подряд дают ноль проверки. Формат его сообщения не увидел никто — я заметил глазами, случайно.
> 
> Чинить надо второе: локальная установка всегда будет чем-то, что можно забыть, а CI — единственный путь, который проходят все коммиты обоих участников. `.vscode/settings.json` содержит только ключ `.windows`, но добавлять `.osx` не буду — блок существует, чтобы отменить перебив в **ваших** user-settings, а на его машине отменять нечего. Это была бы конфигурация на будущее.
> 
> ## Вопрос
> 
> **1. Расширить проверку сообщений в CI на push в `main`, а не только на PR?** — *предлагаю: да.* Ловит именно тот случай, когда локальные хуки не поставлены, и работает для обоих одинаково.
> 
> **2. Ронять `scripts/check.sh`, если `core.hooksPath` не указывает на `.githooks`?** — *предлагаю: да, с пропуском при `CI=true`.* Чинится одной командой, и сообщение назовёт её. Минус: строгость к тому, кто просто хотел прогнать тесты.
> 
> **3. Дописать в README раздел про macOS?** — *предлагаю: да, три строки — Temurin через brew, `scripts/hooks.sh`, проверка `java -version`.* Сейчас README говорит «Requires JDK 21» без указания, как его взять.
> 
> Скажите «всё как предложено» или что иначе.

**Outcome (English, added 2026-09-04, retrospectively)**

> Checked what actually differs on macOS instead of assuming: the scripts are POSIX-clean, and core.filemode defaults to true there, so the executable bits work better than on Windows. Concluded the MacBook changes almost nothing, and that the real gap his commit exposed was a different one - the commit-message convention had no enforcement path at all, because local hooks are installed by hand and CI checked messages only on pull requests while we push straight to main.

**Checks**

- traceability gate: not enabled yet (phase 2)


## 15:34

**Author:** Mikhail Novikov

**Prompt**

> лучше сделай файл для онбординга, чтобы адбирахим мог попросить нейросеть и она ему все настроила для работы с репозиторием и скачала что нужно если потребуется

**Prompt (English, added 2026-09-04, retrospectively)**

> Better, make an onboarding file, so that Abdirakhim can ask an AI and it sets everything up for working with the repository and downloads whatever is needed.

**Outcome**

> **Понял так:** файл, который Абдирахим открывает вместе с ИИ и говорит «настрой», после чего у него рабочее окружение — без того, чтобы вы объясняли устно.
> 
> Одно решение брать не буду: ставить ли софт молча. `docs/ai/security.md` уже говорит, что установка инструмента — решение человека, так что файл будет велеть спросить один раз перед установкой. Это записано, спрашивать нечего.
> 
> Три вещи меняют артефакт, и их решаете вы:
> 
> **1. Что файл покрывает** — только настройку (JDK, git-идентичность, хуки, проверка сборки), или настройку плюс ориентацию (что прочитать первым, как мы работаем, откуда взять первую задачу)? — *предлагаю: настройку плюс короткую ориентацию.* Иначе после «готово» он спросит «а дальше что», и ответ окажется у вас в мессенджере, а не в репозитории.
> 
> **2. Платформы** — только macOS под него, или macOS и Windows? — *предлагаю: обе.* Ваша установка сейчас нигде не записана: JDK ставился zip'ом мимо winget, `make` отсутствует, VS Code перебивал JAVA_HOME. Если ваш ноутбук умрёт, это знание умрёт с ним.
> 
> **3. Чем файл заканчивается** — списком шагов, или проверяемым критерием «настроено»? — *предлагаю: критерием.* Конкретно: `scripts/check.sh` зелёный, `git config core.hooksPath` = `.githooks`, и тестовый коммит с неправильным сообщением **отвергается**. Последнее важнее всего: именно оно не сработало у него, и «я всё сделал» без этой проверки ничего не значит.
> 
> Скажите «всё как предложено» или что иначе.
> 
> Отдельно висит незакрытым мой прошлый вопрос — расширять ли проверку сообщений в CI на push в `main`. Онбординг-файл его не заменяет: файл лечит того, кто его прочитал, а CI ловит всех.

**Outcome (English, added 2026-09-04, retrospectively)**

> Asked three closed questions before writing anything: what the file covers, which platforms, and whether it ends on steps or on criteria. Did not ask about install autonomy, because security.md already settles that installing a tool is the human's decision - cited it instead.

**Checks**

- traceability gate: not enabled yet (phase 2)


## 15:40

**Author:** Mikhail Novikov

**Prompt**

> 1) то что ты предложил
> 2) мак, виндовс, линукс
> 3) критерии 
> да, расширяй

**Prompt (English, added 2026-09-04, retrospectively)**

> 1) what you proposed 2) mac, windows, linux 3) criteria - yes, extend it.

**Outcome**

> Запушено, `main` на `bfa6849`. CI на новом workflow ещё идёт — предыдущий прогон зелёный.
> 
> ## `docs/onboarding.md`
> 
> Написан, чтобы его отдали ассистенту: «work through docs/onboarding.md». Пять шагов, каждый начинается с **проверки**, а не с установки — уже настроенное не переустанавливается.
> 
> Три правила для ассистента вынесены наверх:
> - **спросить один раз** перед установкой, собрав всё недостающее в один вопрос (по `security.md` установка инструмента — решение человека);
> - **не выдумывать имя пакета или версию** — правдоподобная команда, которая ничего не ставит, хуже ошибки;
> - **закончить на критериях**, а не на шагах.
> 
> macOS, Windows и Linux все три. Ваша установка нигде не была записана — JDK ставился zip'ом мимо winget, `make` отсутствует, VS Code перебивал `JAVA_HOME`. Теперь это в репозитории, а не в вашей голове.
> 
> **Четвёртый критерий — тот, ради которого всё писалось:**
> 
> ```sh
> git commit --allow-empty -m "wip: testing the hook"
> ```
> 
> Должен **упасть**. Если прошёл — хуки не установлены, и историю ничто не защищает. Это ровно то, что не сработало у Абдирахима, и «я всё сделал по шагам» без этой проверки ничего не значит. При корректном отказе чистить нечего — коммит не создался.
> 
> Плюс раздел «Now what»: три документа в порядке чтения, как мы работаем, где текущие задачи.
> 
> ## CI на push
> 
> Проверка сообщений больше не только на pull request. Считает диапазон push'а, проверяет **все** сообщения в нём и называет автора каждого плохого — чтобы плохой пуш выдал все проблемы разом, а не по одной за перезапуск.
> 
> Прогнал по последним шести коммитам: падает ровно на `d739d44` Абдирахима и пропускает остальные пять. Исторические коммиты при этом не трогаются — проверяется только диапазон нового пуша.
> 
> ## Почему обе половины
> 
> Локальный хук лечит того, кто его установил. CI проходят все коммиты обоих, независимо от того, что стоит на чьём ноутбуке. Онбординг-файл уменьшает шанс, что хук забудут; CI ловит, когда всё-таки забыли.
> 
> Абдирахиму достаточно сказать: открой репозиторий с ассистентом и попроси пройти `docs/onboarding.md`.

**Outcome (English, added 2026-09-04, retrospectively)**

> Wrote docs/onboarding.md for macOS, Windows and Linux, addressed to an assistant, ending on four criteria of which the last is the point: a deliberately wrong commit message must be rejected, verified rather than assumed. Extended the CI commit-message check to pushes as well as pull requests and ran it over the last six commits, where it fails exactly the one that breaks the convention.

**Checks**

- traceability gate: not enabled yet (phase 2)


## 15:59

**Author:** Mikhail Novikov

**Prompt**

> <ide_opened_file>The user opened the file c:\study\PAI\Exchange-Student-Guide-Platform\docs\ai\journal\2026-09-04-998ab12b.md in the IDE. This may or may not be related to the current task.</ide_opened_file>
> проверь тех долг, записи в журнал должны быть переведены на английский язык (и переводится в ходе промптинга)

**Prompt (English)**

> Check the technical debt. Journal entries must be translated into English, and the translation must happen during the turn.

**Outcome**

> All seven of today's entries had been written with no English rendering: the rule lived in CLAUDE.md and lost to context, the same failure the sharpening rule had. Resolved DEBT-001, whose trigger was a week away: hook english now binds to the session with an entry actually open, ordered by prompt time, which also closes a misattribution risk that was real today because two of us worked at once. Added the mechanism the register had missed - a prompt containing Cyrillic makes the UserPromptSubmit hook demand a rendering in that turn, and the Stop hook writes 'English rendering: NOT supplied' into the entry so an omission leaves a trace. Along the way: caught myself reporting BUILD OK from a pipeline reading tail's exit code while the compile was broken, and caught the first version of this fix refusing outright when several sessions were open, which would have made the command permanently unusable.

**Checks**

- traceability gate: not enabled yet (phase 2)
- English rendering: supplied


## 16:05

**Author:** Mikhail Novikov

**Prompt**

> <ide_opened_file>The user opened the file c:\study\PAI\Exchange-Student-Guide-Platform\CLAUDE.md in the IDE. This may or may not be related to the current task.</ide_opened_file>
> допиши переводы с явной меткой

**Prompt (English)**

> Add the translations, with an explicit label.

**Outcome**

> Backfilled English renderings into the seven entries from 4 September that had none, each labelled 'added 2026-09-04, retrospectively'. Verified the originals survived: 56 lines added, none removed. The outcomes are concise English summaries rather than word-for-word translations of long Russian answers, which is what the --outcome flag was always for. Left the four note entries from 3 September alone, since they were written in English already. Recorded the exception in docs/ai/journal/README.md, so the document no longer contradicts the file: add, never alter, and say when it was added.

**Checks**

- traceability gate: not enabled yet (phase 2)
- English rendering: supplied


## 16:44

**Author:** Mikhail Novikov

**Prompt**

> проведи аудит инструкций, хуков и т.п. еще раз. нужно чтобы как можно больше инструкций и т.п. были гарантированы, а не ты забывал про них. предложи улучшения, изменения

**Prompt (English, added 2026-09-04, retrospectively)**

> Audit the instructions, the hooks and so on once more. As many instructions as possible need to be
> guaranteed rather than left for you to forget. Propose improvements and changes.

**Outcome**

> Запушено, `main` на `26956e6`, 65 тестов зелёные.
> 
> ## Что стало гарантированным
> 
> | Правило | Было | Стало |
> |---|---|---|
> | Не отчитываться «готово», когда не готово | ничего | **Bash-гайка спрашивает**, когда сборка уходит в конвейер, прячущий код возврата |
> | Не обходить проверки | правило чести | **запрет** на `--no-verify`, `-DskipTests` |
> | Нет `TODO` без `DEBT` | ничего | **отказ до того, как правка ляжет** |
> | Документация не описывает несуществующее | `check.sh` | **гейт на `Stop`** — ход не заканчивается |
> | Не добавлять зависимость без спроса | ничего | `pom.xml` защищён |
> | Автор коммита существует в реестре | ничего | `ai-tools authors` в `check.sh` |
> 
> ## Три вещи, которые стоит знать
> 
> **Гайка заблокировала саму себя при первом же применении.** Команда, которая писала документацию **про** `--no-verify`, содержала эту строку — и была отвергнута. Это тот самый «крик волками», о котором я предупреждал в плане, вылезший на первом настоящем прогоне. Сузил: флаг считается использованным, только если в том же сегменте есть и сам инструмент. Тест на этот случай добавлен.
> 
> **Гейт блокирует один раз на причину.** Проверено: подсадил битую ссылку → отказ; вторая попытка без починки → ход проходит, но в журнал пишется `documentation gate: NOT PASSED (1 unresolved; allowed through because it had already blocked once)`. Гейт, способный отказывать вечно, запирает сессию так, что вы не можете вмешаться.
> 
> **`./mvnw verify` в гейт не попал** — 27 секунд на ход. Гейт такой цены выключают, а выключенный не гарантирует ничего. Полный прогон остался в `pre-push` и CI.
> 
> ## Скорость
> 
> `docs-check` 916 → **625 мс** (я обещал ~575, сообщаю фактическое). Причина была не в задаче: `DocsCheck` обходил дерево **четыре раза** и читал каждый файл трижды. Теперь один обход. Плюс флаги короткоживущей JVM — `guard`, который срабатывает на каждой правке, 489 → 435 мс.
> 
> ## Что осталось на честном слове, и почему
> 
> **«Тест раньше кода»** — задним числом тест, написанный первым, неотличим от написанного вторым. **«Не уверен — спроси»** — механизма, измеряющего уверенность, нет. Обе строки теперь так и записаны в `docs/ai/README.md`, вместе с таблицей «что чем держится».
> 
> Там же исправлено ложное утверждение: README заявлял, что правило 6 подкреплено генератором матрицы, которого не существует до фазы 2.
> 
> Не сделал: CI не обновлял — `authors` уже входит в `scripts/check.sh`, который CI и запускает, так что отдельный шаг был бы дублем.

**Outcome (English, added 2026-09-04, retrospectively)**

> Converted six rules that rested on memory into mechanisms: a Bash guard denying `--no-verify` and
> `-DskipTests` and asking whenever a build is piped into a command that hides its exit code; a
> content check refusing a `TODO` with no debt entry before the edit lands; a `Stop` gate on the
> documentation checks; `pom.xml` added to the protected paths; and an authors check wired into
> `scripts/check.sh`. Each was verified by breaking it on purpose. The bypass guard blocked the very
> command that was documenting it on its first real use, and had to be narrowed to require the tool
> in the same segment as the flag. `DocsCheck` turned out to be walking the tree four times; one walk
> took it from 916 to 625 ms. `./mvnw verify` was deliberately left out of the gate at 27 seconds a
> turn, because a gate that slow is one somebody switches off. Recorded in `docs/ai/README.md` which
> rules stay on good faith — test before code, and stop when unsure — and corrected a false claim
> that rule 6 was backed by a matrix generator that does not exist until phase 2.

**Checks**

- traceability gate: not enabled yet (phase 2)
- English rendering: NOT supplied
- documentation gate: passed


## 17:33

**Author:** Mikhail Novikov

**Prompt**

> <ide_opened_file>The user opened the file c:\study\PAI\Exchange-Student-Guide-Platform\docs\roadmap\01-requirements-design.md in the IDE. This may or may not be related to the current task.</ide_opened_file>
> продолжи

**Prompt (English)**

> Look at the last journal entry - it has no translation, and on top of that it is not committed. / Carry on.

**Outcome**

> Two failures from one entry, and both were structural rather than careless. The missing rendering was not a missed reminder: the UserPromptSubmit hook fired, and the Stop hook wrote 'NOT supplied' into the entry - the rule was observed, not enforced. Stop now refuses to end such a turn, once per turn, with the flag set by the hook that reads the prompt so the gate asks for exactly what the reminder asked for. The uncommitted entry was structural too: an entry is appended after the turn's last action, so the turn it describes can never commit it, and nothing owned getting it into history. Against my own recommendation, and on the human's decision, the Stop hook now makes that commit - bounded to a docs/ai/journal pathspec, silent during a merge, a rebase or on a detached HEAD, and retrying rather than losing on failure. Backfilled the missing rendering into the 16:44 entry, labelled retrospective, leaving the NOT-supplied line in place as the record of what happened. Two false exemptions found on the way: -DskipTests=false was refused as a bypass though it switches tests on, and the journal's README was exempt from the protected-path guard along with the entries although it is an instruction file. Seventy-four tests, both gate paths verified end-to-end in a throwaway repository.

**Checks**

- traceability gate: not enabled yet (phase 2)
- English rendering: supplied
- documentation gate: passed

