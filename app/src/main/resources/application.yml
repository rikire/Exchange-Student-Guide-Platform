spring:
  application:
    name: exchange-student-guide
  thymeleaf:
    cache: false
  jpa:
    hibernate:
      ddl-auto: validate

server:
  port: 8080
  error:
    whitelabel:
      enabled: false

logging:
  level:
    root: INFO
    in.ac.iitm.guide: DEBUG
