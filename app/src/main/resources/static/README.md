Static assets: the design tokens taken from the IITM sites (`css/tokens.css`), the site's own styles
(`css/site.css`) and images. No CSS framework: the pages run on the tokens alone.
See docs/design/reference.md.
