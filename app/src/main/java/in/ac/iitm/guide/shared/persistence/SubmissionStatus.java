package in.ac.iitm.guide.shared.persistence;

/** The three values FR-012 shows and nothing else (docs/architecture/data-model.md). */
public enum SubmissionStatus {
    PENDING,
    APPROVED,
    REJECTED
}
