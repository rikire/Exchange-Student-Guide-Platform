<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>in.ac.iitm.guide</groupId>
  <artifactId>exchange-student-guide</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>Exchange Student Guide Platform</name>
  <description>Community-editable knowledge base for incoming exchange students at IIT Madras</description>

  <modules>
    <module>tools</module>
    <module>app</module>
  </modules>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

    <!-- Versions verified against Maven Central on 2026-09-02, not recalled from memory. -->
    <spring-boot.version>3.5.16</spring-boot.version>
    <spring-modulith.version>1.4.13</spring-modulith.version>
    <hibernate-search.version>7.2.6.Final</hibernate-search.version>
    <jackson.version>2.22.2</jackson.version>
    <archunit.version>1.3.0</archunit.version>

    <spotless.version>2.44.0</spotless.version>
    <maven-shade.version>3.6.0</maven-shade.version>
    <maven-surefire.version>3.5.2</maven-surefire.version>
  </properties>

  <build>

    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>com.diffplug.spotless</groupId>
          <artifactId>spotless-maven-plugin</artifactId>
          <version>${spotless.version}</version>
          <configuration>
            <java>
              <palantirJavaFormat/>
              <removeUnusedImports/>
              <trimTrailingWhitespace/>
              <endWithNewline/>
            </java>
            <pom>
              <sortPom>
                <expandEmptyElements>false</expandEmptyElements>
              </sortPom>
            </pom>
          </configuration>
          <executions>
            <execution>
              <goals>
                <goal>check</goal>
              </goals>
              <phase>verify</phase>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${maven-surefire.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-shade-plugin</artifactId>
          <version>${maven-shade.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <!-- Declared here so both modules inherit it: formatting noise in a diff costs the
                 other person review time, and this is the cheapest place to stop it. -->
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
